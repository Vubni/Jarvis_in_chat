# 🤖 Jarvis Chat Assistant

Умный ассистент с поддержкой естественного языка, интеграцией в Telegram, Telethon, WebApp, умный дом, платежные системы и модульную архитектуру.

Документация для пользователей доступна здесь:  
https://vubni.gitbook.io/jarvis/

---

## ✨ Возможности

- Обработка команд и сообщений через **Telegram Bot API** и **Telethon клиентов**
- Поддержка модульной системы (каждый модуль — отдельная команда)
- Встроенная работа с **S3**, **БД**, **оплатами**, **умным домом**, **WebApp**
- Поддержка FSM (aiogram)
- Гибкая архитектура и высокая расширяемость
- Автоматическая очистка временных файлов и хранилищ

---

## 🗂 Структура проекта

```plaintext
├── server.py                # Главный файл: HTTP‑сервер, aiogram‑бот и telethon‑клиенты
├── config.py                # Конфигурация проекта: токены, ключи, параметры
├── core.py                  # Базовые общие функции
├── run.py                   # Управление запуском aiogram и telethon
├── aiogram_run.py           # Отдельный запуск aiogram‑бота
├── clients_run.py           # Запуск Telethon клиентов
├── TelegramClient.py        # Класс клиента Telethon
├── delete_temp              # Очистка старых данных из БД и S3
├── s3.py                    # Работа с S3 хранилищем

├── modules/                 # Модули ассистента
│   └── [module_name]/
│        ├── main.py         # Логика обработки сообщений
│        └── settings.py     # Настройки модуля (уникальный ID, параметры)

├── functions/               # Вспомогательные функции
├── database/
│   └── database.py          # Оптимизированное подключение к БД

├── FSM/                     # Состояния aiogram
├── callback/                # Обработка callback‑запросов
├── inline/                  # Inline‑режим Telegram
├── handlers/                # Текстовые хендлеры
├── keyboards/               # Клавиатуры Telegram
├── payment/                 # Интеграция платежей
├── smart_home/              # HTML‑панель умного дома
├── auth/                    # Авторизация пользователя (HTML)
├── web_app/                 # WebApp логика
└── base_settings/           # Базовые настройки профиля
```
### 🚀 Установка и запуск
Ниже приведён стандартный процесс установки для Python‑проектов.

1. Клонирование репозитория
```bash
git clone https://github.com/username/jarvis.git
cd jarvis
```
2. Создание виртуального окружения
```bash
python -m venv venv
source venv/bin/activate      # Linux / macOS
.\venv\Scripts\activate       # Windows
```
3. Установка зависимостей
```bash
pip install -r requirements.txt
```
4. Настройка проекта
Создайте файл .env или заполните config.py (API ключи, токены и т.д.).

5. Запуск сервера
```bash
python server.py
```
#### После запуска:

1. поднимется локальный сервер,
2. запустится aiogram‑бот,
3. активируются telethon‑клиенты,
4. загрузятся модули.

### 🧩 Структура модулей
Каждый модуль — это мини‑приложение внутри Jarvis.

Пример:
```
modules/
└── weather/
     ├── main.py
     └── settings.py
```
settings.py содержит:

- уникальный код модуля
- описание
- дополнительные параметры

main.py — логика обработки сообщений, API‑запросы, ответы пользователю.

### ⭐ Если проект понравился
Поставь ⭐ на GitHub — это мотивирует развивать Jarvis дальше :)

